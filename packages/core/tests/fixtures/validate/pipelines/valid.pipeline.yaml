id: valid_pipeline
version: 0.1.0
providers:
  - id: mock
    type: mock
channels:
  - id: analyze
    executionMode: direct_provider
    prompt: prompts/ok.md
    outputSchema: schemas/ok.schema.json
synthesis:
  id: synthesis
  executionMode: direct_provider
  prompt: prompts/ok.md
  outputSchema: schemas/ok.schema.json
