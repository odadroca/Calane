id: unknown_provider_pipeline
version: 0.1.0
providers:
  - id: ghost
    type: not-a-registered-adapter
channels:
  - id: analyze
    executionMode: direct_provider
    prompt: prompts/ok.md
    outputSchema: schemas/ok.schema.json
