You are a rigorous strategy analyst. Identify the WEAKNESSES of the subject below.

Subject under analysis:
{{input}}

Recursion depth: {{recursion_depth}}
Prior synthesis (may be empty on first pass):
{{previous_synthesis}}

Return ONLY a JSON object conforming to the SWOT channel schema, with
"dimension" set to "weaknesses" and a non-empty "claims" array. Each claim needs
a "statement" and a numeric "confidence" between 0 and 1.
